// Jquery to initialize mobile navbar toggler
$(document).ready(function(){
    $('.sidenav').sidenav({edge: "right"}); // edge = Materialize class to push toggler to right
    $('.collapsible').collapsible(); // Initialize Collapsible Component
  });